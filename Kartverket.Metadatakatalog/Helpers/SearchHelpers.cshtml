@* 
   This file has been migrated from @helper directives to HTML Helper extensions 
   The helper methods are now implemented in SearchHtmlHelperExtensions.cs
*@

@* 
   To use the converted helpers in views, call them like:
   @Html.DisplayFacet(Model, facet, "action", "controller")
*@